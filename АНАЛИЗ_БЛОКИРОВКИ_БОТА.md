# 🔍 Анализ блокировки бота Telegram

## 📋 Обзор

Этот документ описывает, как проанализировать блокировку бота и определить причину проблемы.

## 🚨 Основные причины блокировки бота

### 1. **Блокировка со стороны Telegram**
- Бот нарушил правила Telegram (спам, злоупотребление API)
- Слишком много жалоб от пользователей
- Подозрительная активность

**Признаки:**
- Сообщение "Из-за злоупотребления нашим сервисом мошенниками..."
- Ошибки `Forbidden` или `Unauthorized` в логах
- Бот не отвечает на команды

### 2. **Проблемы с токеном**
- Токен неверный или устарел
- Токен был отозван
- Токен был скомпрометирован

**Признаки:**
- Ошибки `Unauthorized` в логах
- Бот не запускается
- "Invalid token" в логах

### 3. **Превышение лимитов API (Rate Limiting)**
- Слишком много запросов к Telegram API
- Отправка сообщений слишком быстро
- Массовая рассылка без задержек

**Признаки:**
- Ошибки `Rate limit exceeded`
- Ошибки `Too Many Requests`
- Временные блокировки

### 4. **Конфликт экземпляров**
- Несколько экземпляров бота запущены одновременно
- Старый процесс не был остановлен

**Признаки:**
- Ошибки `Conflict` в логах
- Бот работает нестабильно
- Сообщения дублируются

### 5. **Проблемы с отправкой сообщений**
- Попытки отправить сообщения заблокированным пользователям
- Некорректные chat_id
- Проблемы с форматом сообщений

**Признаки:**
- Ошибки `Chat not found`
- Ошибки `Blocked by user`
- Ошибки `Bad Request`

## 🔧 Инструменты для анализа

### Автоматический анализ

**На локальной машине:**
```bash
python3 analyze_bot_block.py
```

**На сервере (с активацией виртуального окружения):**
```bash
cd /opt/taxi-zhukovo
source venv/bin/activate
python3 analyze_bot_block.py
```

Или используйте скрипт-обертку:
```bash
cd /opt/taxi-zhukovo
bash analyze_bot_block.sh
```

Скрипт проверит:
- ✅ Конфигурацию бота
- ✅ Базу данных
- ✅ Логи на наличие ошибок
- ✅ Паттерны ошибок
- ✅ Активность бота

### Ручная диагностика

#### Шаг 1: Проверка локальной конфигурации

```bash
# Проверка токена
grep TELEGRAM_BOT_TOKEN .env

# Проверка наличия .env файла
ls -la .env
```

#### Шаг 2: Проверка базы данных

**На локальной машине:**
```bash
python3 check_bot_issue.py
```

**На сервере:**
```bash
cd /opt/taxi-zhukovo
source venv/bin/activate
python3 check_bot_issue.py
```

Это покажет:
- Количество пользователей без телефона
- Количество водителей
- Статистику по заказам

#### Шаг 3: Проверка логов на сервере

**Через SSH:**
```bash
# Подключение к серверу
ssh root@195.133.73.49

# Переход в директорию проекта
cd /opt/taxi-zhukovo

# Проверка статуса бота
systemctl status taxi-bot

# Просмотр последних логов
journalctl -u taxi-bot -n 200 --no-pager

# Поиск ошибок
journalctl -u taxi-bot -n 500 | grep -iE "error|forbidden|unauthorized|conflict|rate"

# Логи в реальном времени
journalctl -u taxi-bot -f
```

**Проверка файлов логов:**
```bash
# Основной лог
tail -100 /opt/taxi-zhukovo/logs/bot.log

# Лог ошибок
tail -100 /opt/taxi-zhukovo/logs/bot_error.log

# Поиск конкретных ошибок
grep -i "forbidden\|unauthorized\|conflict" /opt/taxi-zhukovo/logs/bot_error.log
```

#### Шаг 4: Проверка BotFather

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота
4. Проверьте статус бота
5. Проверьте, не было ли жалоб

#### Шаг 5: Проверка запущенных процессов

```bash
# На сервере
ps aux | grep run.py
ps aux | grep python | grep bot

# Проверка systemd службы
systemctl status taxi-bot
systemctl list-units | grep taxi
```

## 📊 Анализ конкретных ошибок

### Ошибка: `Unauthorized`

**Причина:** Проблема с токеном бота

**Решение:**
1. Проверьте токен в `.env` файле
2. Проверьте токен в BotFather
3. Если токен неверный, получите новый в BotFather
4. Обновите `.env` файл
5. Перезапустите бота

### Ошибка: `Forbidden`

**Причина:** Telegram заблокировал бота

**Решение:**
1. Проверьте статус в BotFather
2. Проверьте, не было ли жалоб
3. Обратитесь в поддержку Telegram
4. Проверьте, не нарушает ли бот правила

### Ошибка: `Conflict`

**Причина:** Несколько экземпляров бота запущены одновременно

**Решение:**
```bash
# На сервере
# 1. Остановите все процессы
pkill -f run.py
systemctl stop taxi-bot

# 2. Проверьте, что процессы остановлены
ps aux | grep run.py

# 3. Запустите бота снова
systemctl start taxi-bot
systemctl status taxi-bot
```

### Ошибка: `Rate limit exceeded` / `Too Many Requests`

**Причина:** Превышен лимит запросов к API

**Решение:**
1. Добавьте задержки между запросами
2. Уменьшите частоту отправки сообщений
3. Проверьте, нет ли зацикленных запросов
4. Используйте очередь для отправки сообщений

### Ошибка: `Chat not found` / `Blocked by user`

**Причина:** Пользователь заблокировал бота или удалил чат

**Решение:**
1. Это нормальная ситуация - добавьте обработку в код
2. Не пытайтесь отправлять сообщения заблокированным пользователям
3. Проверяйте доступность пользователя перед отправкой

## 🔍 Детальный анализ логов

### Поиск паттернов ошибок

```bash
# Все ошибки за последний час
journalctl -u taxi-bot --since "1 hour ago" | grep -i error

# Ошибки Telegram API
journalctl -u taxi-bot -n 1000 | grep -iE "telegram|api|forbidden|unauthorized"

# Конфликты
journalctl -u taxi-bot -n 1000 | grep -i conflict

# Rate limiting
journalctl -u taxi-bot -n 1000 | grep -iE "rate|limit|too many"
```

### Анализ частоты ошибок

```bash
# Подсчет ошибок по типам
journalctl -u taxi-bot --since "24 hours ago" | grep -i error | sort | uniq -c | sort -rn
```

### Поиск критических моментов

```bash
# Логи вокруг времени блокировки
journalctl -u taxi-bot --since "2025-01-15 10:00:00" --until "2025-01-15 11:00:00"
```

## 📝 Чек-лист диагностики

- [ ] Запущен скрипт `analyze_bot_block.py`
- [ ] Проверен токен в `.env`
- [ ] Проверен статус бота в BotFather
- [ ] Проверены логи на сервере
- [ ] Проверен статус systemd службы
- [ ] Проверены запущенные процессы
- [ ] Проверена база данных
- [ ] Проверена активность бота (последние заказы)
- [ ] Проверены онлайн водители
- [ ] Найдены и проанализированы все ошибки в логах

## 🛠️ Быстрое решение проблем

### Если бот не отвечает

1. **Проверьте статус:**
   ```bash
   systemctl status taxi-bot
   ```

2. **Перезапустите бота:**
   ```bash
   systemctl restart taxi-bot
   ```

3. **Проверьте логи:**
   ```bash
   journalctl -u taxi-bot -n 50
   ```

### Если бот заблокирован Telegram

1. **Проверьте BotFather:**
   - Откройте @BotFather
   - Проверьте статус бота
   - Проверьте жалобы

2. **Обратитесь в поддержку Telegram:**
   - Опишите проблему
   - Приложите логи
   - Объясните, что бот делает

3. **Проверьте правила:**
   - Убедитесь, что бот не нарушает правила Telegram
   - Проверьте, нет ли спама
   - Проверьте частоту запросов

### Если проблема с токеном

1. **Получите новый токен:**
   - Откройте @BotFather
   - Отправьте `/token`
   - Выберите вашего бота
   - Скопируйте новый токен

2. **Обновите конфигурацию:**
   ```bash
   # На сервере
   nano /opt/taxi-zhukovo/.env
   # Обновите TELEGRAM_BOT_TOKEN
   ```

3. **Перезапустите бота:**
   ```bash
   systemctl restart taxi-bot
   ```

## 📞 Получение помощи

Если проблема не решена:

1. **Соберите информацию:**
   ```bash
   # Сохраните логи
   journalctl -u taxi-bot -n 500 > /tmp/bot_logs.txt
   
   # Сохраните результаты анализа
   cd /opt/taxi-zhukovo
   source venv/bin/activate
   python3 analyze_bot_block.py > /tmp/bot_analysis.txt
   
   # Сохраните конфигурацию (без токена!)
   grep -v TOKEN .env > /tmp/bot_config.txt
   ```

2. **Проверьте документацию:**
   - `ДИАГНОСТИКА_ПРОБЛЕМЫ.md`
   - `РЕШЕНИЕ_ПРОБЛЕМЫ_СТАРТ.md`
   - `README.md`

3. **Обратитесь за помощью:**
   - Приложите все собранные логи
   - Опишите проблему подробно
   - Укажите, когда проблема началась

## 🔗 Полезные ссылки

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [Telegram Bot FAQ](https://core.telegram.org/bots/faq)
- [BotFather](https://t.me/botfather)
- [Telegram Support](https://telegram.org/support)

---

**Дата создания:** 2025-01-15  
**Версия:** 1.0

