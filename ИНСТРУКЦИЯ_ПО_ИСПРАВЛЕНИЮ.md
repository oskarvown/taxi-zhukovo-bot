# 📘 Инструкция по исправлению проблемы с заказами

## 🎯 Быстрый старт

Если водители не видят свои заказы в истории, выполните эти шаги:

### Шаг 1: Полная диагностика
Двойной клик по файлу:
```
ПОЛНАЯ_ДИАГНОСТИКА.bat
```

Это проверит всю систему и покажет, где именно проблема.

### Шаг 2: Исправление "зависших" заказов
Если в диагностике найдены заказы в статусе "accepted" или "in_progress":
```
ИСПРАВИТЬ_ЗАВИСШИЕ.bat
```

Это завершит заказы, которые водители приняли, но не завершили через кнопку в боте.

### Шаг 3: Проверка базы данных
Для детального отчета о БД:
```
ПРОВЕРКА_БД.bat
```

Откроется файл `DB_CHECK_RESULT.txt` с подробной информацией.

---

## 📋 Понимание проблемы

### Почему заказы не видны в истории?

История водителя показывает **ТОЛЬКО ЗАВЕРШЕННЫЕ** заказы (status = 'completed').

**Полный цикл заказа:**
1. 🆕 `pending` - клиент создал заказ, ждет водителя
2. ✅ `accepted` - водитель нажал "Принять заказ"
3. 🚗 `in_progress` - водитель нажал "Начать поездку"
4. ✔️ `completed` - водитель нажал "Завершить" → **ТОЛЬКО ТЕПЕРЬ В ИСТОРИИ!**

### Частые причины проблемы

#### 1. Заказы не завершены
**Признак:** Водитель принял заказ, но не нажал "Завершить"

**Решение:** Запустить `ИСПРАВИТЬ_ЗАВИСШИЕ.bat`

#### 2. Отсутствуют поля в БД
**Признак:** Ошибки при создании/принятии заказов

**Решение:**
```bash
python apply_migration.py
```

#### 3. Несоответствие статистики
**Признак:** В профиле водителя 0 поездок, но заказы были

**Решение:** Запустить `ИСПРАВИТЬ_ЗАВИСШИЕ.bat` (обновит статистику)

---

## 🔧 Доступные инструменты

### 1. ПОЛНАЯ_ДИАГНОСТИКА.bat ⭐ РЕКОМЕНДУЕТСЯ
**Что делает:**
- Проверяет структуру БД
- Проверяет целостность данных
- Проверяет код
- Показывает статистику по водителям

**Когда использовать:** Всегда начинайте с этого!

### 2. ПРОВЕРКА_БД.bat
**Что делает:**
- Проверяет структуру таблиц
- Показывает статистику заказов
- Показывает информацию о водителях
- Сохраняет отчет в `DB_CHECK_RESULT.txt`

**Когда использовать:** Для детального анализа БД

### 3. ИСПРАВИТЬ_ЗАВИСШИЕ.bat
**Что делает:**
- Находит заказы в статусах `accepted` и `in_progress`
- Предлагает завершить их автоматически
- Обновляет статистику водителей

**Когда использовать:** Если диагностика показала "зависшие" заказы

### 4. ИСПРАВИТЬ_ЗАКАЗЫ.bat
**Что делает:**
- Запускает быструю проверку
- Затем полную диагностику с тестированием

**Когда использовать:** Комплексная проверка и исправление

---

## 🐛 Диагностика вручную

### Проверка через SQLite

Если у вас установлен DB Browser for SQLite:

1. Откройте файл `taxi_zhukovo.db`
2. Выполните запросы:

```sql
-- Сколько заказов у водителя?
SELECT COUNT(*) FROM orders WHERE driver_id = <USER_ID>;

-- Какие статусы у заказов?
SELECT id, status, driver_id, created_at, completed_at 
FROM orders 
ORDER BY id DESC LIMIT 10;

-- Статистика водителей
SELECT u.full_name, d.total_rides,
       (SELECT COUNT(*) FROM orders 
        WHERE driver_id = d.user_id AND status = 'completed') as actual
FROM drivers d
JOIN users u ON d.user_id = u.id;
```

### Проверка через Python

```python
python check_driver_history.py
```

Или запустите Python и выполните:

```python
import sqlite3

conn = sqlite3.connect('taxi_zhukovo.db')
cursor = conn.cursor()

# Заказы водителя с user_id = 123
cursor.execute("""
    SELECT id, status, price, created_at, completed_at
    FROM orders 
    WHERE driver_id = 123
""")

for row in cursor.fetchall():
    print(row)

conn.close()
```

---

## ✅ Пошаговое решение

### Сценарий 1: Водитель не видит завершенные заказы

1. Запустите `ПОЛНАЯ_ДИАГНОСТИКА.bat`
2. Проверьте секцию "ПРОВЕРКА РЕАЛЬНЫХ ДАННЫХ"
3. Найдите своего водителя в списке
4. Посмотрите "В БД: X завершенных заказов"

**Если X = 0:** Заказы не завершены
- Решение: `ИСПРАВИТЬ_ЗАВИСШИЕ.bat`

**Если X > 0:** Заказы есть, но не отображаются
- Проверьте код в `bot/handlers/driver.py`, функция `driver_orders`
- Убедитесь, что используется правильный `user_id`

### Сценарий 2: Ошибки при принятии заказов

1. Запустите бота: `ЗАПУСК_БОТА.bat`
2. Смотрите логи в консоли
3. При принятии заказа водителем должны быть логи:
   ```
   🔔 accept_order_callback вызван!
   ✓ Водитель: Имя (ID: 123)
   ✅ Заказ #456 принят водителем Имя
   ```

**Если логов нет:**
- Проверьте, что кнопка "Принять" работает
- Проверьте callback handlers в коде

**Если есть ошибки:**
- Запустите `python apply_migration.py`
- Проверьте, что все зависимости установлены

### Сценарий 3: Статистика водителя не обновляется

1. Запустите `ПОЛНАЯ_ДИАГНОСТИКА.bat`
2. Найдите несоответствия в разделе "ПРОВЕРКА ЦЕЛОСТНОСТИ ДАННЫХ"
3. Запустите `ИСПРАВИТЬ_ЗАВИСШИЕ.bat`
4. Подтвердите исправление (введите `yes`)
5. Статистика обновится автоматически

---

## 🎓 Понимание системы

### Таблица orders

Ключевые поля:
- `id` - номер заказа
- `customer_id` - ID пользователя-клиента (из таблицы users)
- `driver_id` - ID пользователя-водителя (из таблицы users) ⚠️ **ВАЖНО**
- `status` - статус заказа
- `accepted_at` - когда водитель принял
- `completed_at` - когда заказ завершен

### Таблица drivers

Ключевые поля:
- `id` - ID записи водителя
- `user_id` - ID пользователя (связь с таблицей users)
- `total_rides` - счетчик поездок

⚠️ **ВАЖНО:** В таблице `orders` хранится `user_id` водителя, а не `driver.id`!

### Связь между таблицами

```
users.id --> orders.driver_id
users.id --> drivers.user_id
```

Поэтому при запросе истории используется:
```python
Order.driver_id == user.id  # НЕ driver.id!
```

---

## 📝 Проверочный список

После исправления проверьте:

- [ ] База данных имеет все необходимые поля
- [ ] Нет "зависших" заказов (accepted/in_progress)
- [ ] Статистика водителей соответствует реальным данным
- [ ] Бот запускается без ошибок
- [ ] Водитель может принять заказ
- [ ] Водитель может начать поездку
- [ ] Водитель может завершить поездку
- [ ] Клиент может оценить поездку
- [ ] Заказ появляется в разделе "Мои заказы" у водителя

---

## 🆘 Если ничего не помогло

1. Соберите логи:
   - Запустите `ПОЛНАЯ_ДИАГНОСТИКА.bat`, скопируйте результаты
   - Запустите `ПРОВЕРКА_БД.bat`, сохраните `DB_CHECK_RESULT.txt`
   - Запустите бота, скопируйте логи при принятии заказа

2. Проверьте файлы:
   - `bot/models/order.py` - модель заказа
   - `bot/services/order_service.py` - логика работы с заказами
   - `bot/handlers/driver.py` - обработчики команд водителя

3. Откройте Issue на GitHub или обратитесь к разработчику

---

## 📚 Дополнительные ресурсы

- `РЕШЕНИЕ_ПРОБЛЕМЫ_ЗАКАЗОВ.md` - детальное описание проблемы и решений
- `ARCHITECTURE.md` - архитектура проекта
- `SCHEMA_ПРОЕКТА.md` - схема базы данных
- Логи бота в консоли при запуске

---

**Последнее обновление:** 2025-11-07

**Версия системы:** 1.1.0

Удачи в исправлении! 🚀

