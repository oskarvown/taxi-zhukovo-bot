# 🔑 ОБНОВЛЕНИЕ ТОКЕНА TELEGRAM БОТА

## Новый токен
```
8460587651:AAGKey9Z54B2fcgyKTs06Lm5PFveNBVwdpI
```

---

## 🚀 БЫСТРЫЙ СПОСОБ (автоматический скрипт)

```bash
ssh root@195.133.73.49
cd /opt/taxi-zhukovo
git pull
bash deploy/update_token.sh
```

---

## 📋 РУЧНОЙ СПОСОБ (по шагам)

### 1️⃣ Подключаемся к серверу
```bash
ssh root@195.133.73.49
```

### 2️⃣ Переходим в директорию проекта
```bash
cd /opt/taxi-zhukovo
```

### 3️⃣ Обновляем код из GitHub
```bash
git pull origin main
```

### 4️⃣ Редактируем файл .env
```bash
nano .env
```

Находим строку:
```
TELEGRAM_BOT_TOKEN=8447881195:...старый_токен...
```

Заменяем на:
```
TELEGRAM_BOT_TOKEN=8460587651:AAGKey9Z54B2fcgyKTs06Lm5PFveNBVwdpI
```

Сохраняем: `Ctrl+O`, `Enter`, `Ctrl+X`

### 5️⃣ Перезапускаем бота
```bash
sudo systemctl restart taxi-bot
```

### 6️⃣ Проверяем статус
```bash
sudo systemctl status taxi-bot
```

Должно быть:
```
Active: active (running)
```

### 7️⃣ Проверяем логи
```bash
tail -50 logs/bot.log
```

или

```bash
sudo journalctl -u taxi-bot -n 50
```

---

## ✅ ПРОВЕРКА РАБОТЫ

1. Откройте нового бота в Telegram
2. Отправьте `/start`
3. Бот должен ответить приветственным сообщением

---

## ⚠️ ВАЖНО

- После смены токена **старый бот перестанет работать**
- Все пользователи должны начать общение с **новым** ботом
- База данных и история заказов сохраняются

---

## 🆘 Если что-то пошло не так

### Проверить ошибки:
```bash
sudo journalctl -u taxi-bot -n 100 --no-pager | grep -i error
```

### Проверить токен в .env:
```bash
grep TELEGRAM_BOT_TOKEN /opt/taxi-zhukovo/.env
```

### Перезапустить с нуля:
```bash
sudo systemctl stop taxi-bot
sleep 2
sudo systemctl start taxi-bot
sudo systemctl status taxi-bot
```

