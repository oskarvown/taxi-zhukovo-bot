# 🚀 Быстрый анализ блокировки бота

## ⚡ Быстрый старт (3 шага)

### 1. Запустите автоматический анализ

**На локальной машине:**
```bash
python3 analyze_bot_block.py
```

**На сервере:**
```bash
cd /opt/taxi-zhukovo
source venv/bin/activate
python3 analyze_bot_block.py
```

Или используйте скрипт-обертку:
```bash
cd /opt/taxi-zhukovo
bash analyze_bot_block.sh
```

Скрипт автоматически проверит все возможные причины блокировки и выдаст детальный отчет.

### 2. Проверьте логи на сервере

```bash
# Подключитесь к серверу
ssh root@195.133.73.49

# Проверьте статус бота
systemctl status taxi-bot

# Посмотрите последние ошибки
journalctl -u taxi-bot -n 100 | grep -i error
```

### 3. Проверьте BotFather

1. Откройте [@BotFather](https://t.me/botfather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Проверьте статус

## 🔍 Основные причины блокировки

| Причина | Признаки | Решение |
|---------|----------|---------|
| **Telegram заблокировал** | `Forbidden`, сообщение о мошенниках | Проверить BotFather, обратиться в поддержку |
| **Неверный токен** | `Unauthorized` | Обновить токен в `.env` |
| **Превышен лимит** | `Rate limit`, `Too Many Requests` | Добавить задержки, уменьшить частоту |
| **Конфликт экземпляров** | `Conflict` | Остановить все процессы, перезапустить |
| **Пользователь заблокировал** | `Blocked`, `Chat not found` | Нормально, добавить обработку в код |

## 📋 Чек-лист

- [ ] Запущен `analyze_bot_block.py`
- [ ] Проверен токен в `.env`
- [ ] Проверен статус в BotFather
- [ ] Проверены логи на сервере
- [ ] Проверен статус systemd службы

## 📚 Подробная документация

Для детального анализа см. файл: **АНАЛИЗ_БЛОКИРОВКИ_БОТА.md**

---

**Время анализа:** ~2-3 минуты  
**Требуется:** Python 3, доступ к БД, SSH доступ к серверу (для логов)

