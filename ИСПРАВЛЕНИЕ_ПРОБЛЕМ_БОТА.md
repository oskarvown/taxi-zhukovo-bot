# 🔧 Исправление проблем с ботом

## Если бот не работает после обновления

### 1. Проверка на сервере

Выполните диагностику:

```bash
# На сервере
cd /путь/к/проекту/TAXI_ЖУКОВО

# Запустить диагностику
python3 check_bot_fix.py
```

### 2. Основные проблемы и решения

#### Проблема: `DriverZone` не найден

**Ошибка:**
```
ImportError: cannot import name 'DriverZone' from 'bot.models'
```

**Решение:**
1. Обновите код из GitHub:
   ```bash
   git pull origin main
   ```

2. Проверьте, что в `bot/models/__init__.py` есть:
   ```python
   from .driver import Driver, DriverStatus, DriverZone
   ...
   "DriverZone",
   ```

#### Проблема: Бот не запускается

**Проверьте:**
1. Логи запуска:
   ```bash
   python3 run.py
   # или
   tail -f bot.log
   ```

2. Зависимости установлены:
   ```bash
   pip install -r requirements.txt
   ```

3. База данных доступна:
   ```bash
   ls -la taxi_zhukovo.db
   ```

#### Проблема: Ошибки при импорте

**Проверьте импорты:**
```bash
python3 -c "from bot.models import DriverZone; print('OK')"
python3 -c "from bot.handlers.admin import admin_reset_drivers; print('OK')"
python3 -c "from bot.services.queue_manager import queue_manager; print('OK')"
```

### 3. Откат изменений (если ничего не помогает)

```bash
# Вернуться к предыдущему коммиту
git reset --hard HEAD~1

# Или к конкретному коммиту до аудита
git reset --hard 028031d

# Восстановить БД из бэкапа
cp backups/taxi_zhukovo_backup_YYYYMMDD_HHMMSS.db taxi_zhukovo.db

# Перезапустить бота
sudo systemctl restart taxi-bot
```

### 4. Быстрое исправление

Если проблема только с `DriverZone`:

```bash
# На сервере
cd /путь/к/проекту/TAXI_ЖУКОВО

# Отредактировать файл
nano bot/models/__init__.py

# Добавить в импорты:
# from .driver import Driver, DriverStatus, DriverZone

# Добавить в __all__:
# "DriverZone",

# Сохранить и перезапустить
sudo systemctl restart taxi-bot
```

### 5. Проверка логов

```bash
# Если используете systemd
sudo journalctl -u taxi-bot -f

# Или смотреть файл логов
tail -f bot.log

# Искать ошибки
grep -i error bot.log
grep -i traceback bot.log
```

---

## 📋 Чек-лист диагностики

- [ ] Код обновлен: `git pull origin main`
- [ ] Файл `bot/models/__init__.py` содержит `DriverZone`
- [ ] Импорты работают: `python3 check_bot_fix.py`
- [ ] База данных доступна
- [ ] Зависимости установлены
- [ ] Бот запускается без ошибок
- [ ] Логи не содержат критических ошибок

---

**Если проблема не решена**, пришлите:
1. Вывод `python3 check_bot_fix.py`
2. Последние строки логов бота
3. Сообщение об ошибке (если есть)




