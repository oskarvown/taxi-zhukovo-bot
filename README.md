# 🚖 Taxi Жуково Bot

Telegram бот для вызова такси в городе Жуково и прилегающих районах.

## ✨ Что нового в v1.1.0

**🔔 Push-уведомления для водителей!**
- Водители теперь **мгновенно получают уведомления** о новых заказах
- Не нужно вручную проверять наличие заказов
- Первый принявший заказ - получает клиента
- Полное логирование всех уведомлений

👉 Подробнее: [CHANGELOG.md](CHANGELOG.md) | Тестирование: [ТЕСТИРОВАНИЕ_УВЕДОМЛЕНИЙ.md](ТЕСТИРОВАНИЕ_УВЕДОМЛЕНИЙ.md)

---

## 📋 Описание

Это полнофункциональный бот для заказа такси через Telegram. Система включает функционал для клиентов, водителей и администраторов.

### Возможности

#### Для клиентов:
- 🚖 Заказ такси с указанием адресов
- 📍 Поддержка геолокации и текстовых адресов
- 💰 Автоматический расчет стоимости поездки
- 📋 История заказов
- ⭐ Оценка водителей

#### Для водителей:
- 🟢 Управление статусом (онлайн/оффлайн)
- 📱 **Автоматические push-уведомления о новых заказах** ✨ NEW!
- ✅ Принятие и выполнение заказов
- 📊 Статистика поездок и рейтинг

#### Для администраторов:
- 👥 Управление пользователями
- ✅ Верификация водителей
- 📊 Статистика системы
- 📋 Мониторинг заказов

## 🚀 Установка

### Требования

- Python 3.10 или выше
- Git
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))

### Шаг 1: Клонирование репозитория

```bash
cd d:\TAXI_ЖУКОВО
```

### Шаг 2: Создание виртуального окружения

```bash
python -m venv venv
```

Активация виртуального окружения:

**Windows (PowerShell):**
```powershell
.\venv\Scripts\Activate.ps1
```

**Windows (CMD):**
```cmd
venv\Scripts\activate.bat
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 4: Настройка переменных окружения

1. Скопируйте файл `.env.example` в `.env`:

```bash
copy .env.example .env
```

2. Откройте файл `.env` и заполните необходимые данные:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
ADMIN_TELEGRAM_IDS=123456789,987654321
```

#### Как получить Bot Token:
1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env`

#### Как узнать свой Telegram ID:
1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте `/start`
3. Скопируйте ваш ID

### Шаг 5: Запуск бота

```bash
python -m bot.main
```

После успешного запуска вы увидите сообщение:
```
✅ Бот запущен и работает!
```

## 📖 Использование

### Для клиентов

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите "🚖 Заказать такси"
4. Укажите адрес отправления и назначения
5. Подтвердите заказ
6. Ожидайте водителя

### Для водителей

1. Обратитесь к администратору для регистрации
2. После верификации отправьте `/start`
3. Нажмите "🟢 Я на линии"
4. Принимайте заказы
5. Выполняйте поездки

### Для администраторов

Доступные команды:
- `/admin_stats` - Статистика системы
- `/list_drivers` - Список водителей
- `/verify_driver <telegram_id>` - Верифицировать водителя
- `/pending_orders` - Ожидающие заказы

## 🗂️ Структура проекта

```
taxi-zhukovo-bot/
├── .spacekit.constitution      # Спецификация проекта
├── bot/
│   ├── __init__.py
│   ├── main.py                 # Точка входа
│   ├── config.py               # Конфигурация
│   ├── handlers/               # Обработчики команд
│   │   ├── user.py
│   │   ├── driver.py
│   │   └── admin.py
│   ├── models/                 # Модели данных
│   │   ├── user.py
│   │   ├── driver.py
│   │   └── order.py
│   ├── services/               # Бизнес-логика
│   │   ├── order_service.py
│   │   ├── pricing_service.py
│   │   └── user_service.py
│   └── utils/                  # Утилиты
│       ├── keyboards.py
│       └── validators.py
├── database/
│   ├── db.py                   # Настройка БД
│   └── __init__.py
├── requirements.txt
├── .env.example
├── .gitignore
└── README.md
```

## ⚙️ Конфигурация

### Настройка цен

В файле `.env` можно настроить параметры ценообразования:

```env
BASE_PRICE=100          # Базовая цена (руб)
PRICE_PER_KM=25        # Цена за километр (руб)
MIN_PRICE=150          # Минимальная стоимость поездки (руб)
```

### Настройка зоны обслуживания

```env
SERVICE_AREA_LAT=55.5833    # Широта центра зоны
SERVICE_AREA_LON=36.7500    # Долгота центра зоны
SERVICE_RADIUS_KM=50        # Радиус зоны (км)
```

## 🛠️ Разработка

### База данных

По умолчанию используется SQLite. Для продакшн-окружения рекомендуется PostgreSQL:

```env
DATABASE_URL=postgresql://user:password@localhost/taxi_zhukovo
```

### Логирование

Уровень логирования настраивается в `.env`:

```env
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
```

## 📝 Примечания

- Бот использует упрощенный расчет расстояний (формула Haversine)
- Для продакшн-окружения рекомендуется интегрировать API геокодирования (Yandex Maps, Google Maps)
- Для более точного расчета маршрутов можно добавить интеграцию с картами
- Рекомендуется настроить webhook для продакшн-окружения вместо polling

## 🔒 Безопасность

- Никогда не коммитьте файл `.env` в репозиторий
- Храните Bot Token в безопасности
- Регулярно обновляйте зависимости
- Используйте HTTPS для webhook в продакшн

## 🐛 Troubleshooting

### Бот не запускается

1. Проверьте правильность Bot Token
2. Убедитесь, что все зависимости установлены
3. Проверьте файл `.env`

### База данных не создается

1. Убедитесь, что папка для БД существует
2. Проверьте права доступа
3. Проверьте путь в `DATABASE_URL`

### Команды не работают

1. Перезапустите бота
2. Проверьте логи
3. Убедитесь, что пользователь есть в БД

## 📞 Поддержка

По всем вопросам обращайтесь к администратору проекта.

## 📄 Лицензия

MIT License

---

Разработано с ❤️ для города Жуково

