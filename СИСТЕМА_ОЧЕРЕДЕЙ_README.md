# Система Очередей — Документация

## 📋 Обзор

Реализована полная система очередей для распределения заказов между водителями по зонам обслуживания.

## 🎯 Основные возможности

### Для водителей
- **6 зон обслуживания**: Новое Жуково, Старое Жуково, Мысовцево, Авдон, Уптино, Дёма
- **FIFO очередь**: кто раньше вышел на линию — получает заказы первым
- **Статусы**: offline, online, pending_acceptance, busy
- **30 секунд** на принятие решения по заказу
- Автоматический возврат в хвост очереди при отказе/таймауте

### Для клиентов
- Заказы отправляются водителям из выбранной зоны в порядке очереди
- **Fallback через 3 минуты**: если никто не принял, поиск расширяется на все зоны
- Автоматическое уведомление при принятии заказа водителем

## 🗂️ Структура файлов

### Модели
- `bot/models/driver.py` — добавлены `DriverStatus`, `DriverZone`, поля `status`, `current_zone`, `online_since`, `pending_order_id`, `pending_until`
- `bot/models/order.py` — добавлены `OrderZone`, новые статусы `NEW`, `ASSIGNED`, `ACCEPTED`, `FALLBACK`, `EXPIRED`, поле `zone`

### Константы
- `bot/constants.py` — `ZONES`, `PUBLIC_ZONE_LABELS`, `ZONE_KEY_MAP`, таймауты

### Сервисы
- `bot/services/queue_manager.py` — управление очередями водителей по зонам
- `bot/services/scheduler.py` — планировщик таймеров (30s/180s)
- `bot/services/order_dispatcher.py` — диспетчер распределения заказов

### Хэндлеры
- `bot/handlers/driver_queue.py` — обработка действий водителя (выход на линию, принятие/отклонение заказов)
- `bot/handlers/user_queue.py` — интеграция для клиентов (маппинг зон, запуск диспетчера)

### Миграция
- `database/migrations/add_queue_system.py` — миграция БД для новых полей

## 🚀 Установка

### 1. Резервная копия
```bash
# Создайте копию базы данных
copy taxi_zhukovo.db taxi_zhukovo_backup.db
```

### 2. Запуск миграции
```bash
# Windows
ЗАПУСК_МИГРАЦИИ_ОЧЕРЕДЕЙ.bat

# Или напрямую через Python
python database/migrations/add_queue_system.py
```

### 3. Проверка
Миграция добавит:
- Поля `status`, `current_zone`, `online_since`, `pending_order_id`, `pending_until` в таблицу `drivers`
- Поля `zone`, `assigned_driver_id` в таблицу `orders`
- Установит начальные значения для существующих записей

### 4. Запуск бота
```bash
# Windows
ЗАПУСК_БОТА.bat

# Или напрямую
python run.py
```

## 📖 Как работает очередь

### Сценарий 1: Водитель выходит на линию
1. Водитель нажимает "🟢 Я на линии"
2. Выбирает одну из 6 зон
3. `status = online`, `current_zone = выбранная_зона`, `online_since = now`
4. Добавляется в хвост очереди своей зоны

### Сценарий 2: Клиент создаёт заказ
1. Клиент выбирает район (например, "Новое Жуково")
2. Заказ создаётся со статусом `NEW` и `zone = NEW_ZHUKOVO`
3. Запускается глобальный таймер **180 секунд**
4. Диспетчер получает первого водителя из очереди этой зоны

### Сценарий 3: Назначение водителю
1. Водитель: `status = pending_acceptance`, `pending_order_id = order.id`, `pending_until = now + 30s`
2. Водитель удаляется из очереди (временно)
3. Отправляется уведомление с кнопками "Принять" / "Отклонить"
4. Запускается персональный таймер **30 секунд**

### Сценарий 4: Водитель принимает
1. `status = busy`, заказ: `status = accepted`, `driver_id = водитель`
2. Останавливаются все таймеры
3. Клиент получает уведомление

### Сценарий 5: Водитель отклоняет / таймаут
1. `status = online`, `online_since = now` (обновляется!)
2. Водитель возвращается в **хвост** очереди своей зоны
3. Заказ назначается следующему водителю

### Сценарий 6: Fallback (прошло 3 минуты)
1. Если никто не принял за 180 секунд → `status = fallback`
2. Собираются **все** онлайн водители из **всех** зон
3. Сортируются по `online_since` (глобальная FIFO)
4. Повторяется процесс назначения
5. Если все отказались → `status = expired`

## 🎮 UI для водителя

### Меню
- 🟢 Я на линии → выбор зоны
- 🔴 Я оффлайн → выход из очереди
- 📋 Мои заказы
- 📊 Статистика

### Выбор зоны
```
📍 Новое Жуково    📍 Старое Жуково
📍 Мысовцево       📍 Авдон
📍 Уптино          📍 Дёма
🔙 Назад
```

### Уведомление о заказе
```
🚖 НОВЫЙ ЗАКАЗ #123

🏘 Район: Новое Жуково
📍 Откуда: ул. Ленина, 10
📍 Куда: ул. Победы, 25
💰 Цена: 250 руб.

⏱ У вас 30 секунд для ответа

[✅ Принять]  [❌ Отклонить]
```

## 🔧 Технические детали

### Таймеры
- **30 секунд** — ответ водителя (персональный, отменяемый)
- **180 секунд** — глобальный таймер заказа (до fallback)

### Очереди в памяти
```python
{
  "NEW_ZHUKOVO": [driver_id_1, driver_id_2, ...],
  "OLD_ZHUKOVO": [driver_id_5, ...],
  ...
}
```

### Восстановление при перезапуске
При старте бота:
1. Читаются все водители со `status = online` из БД
2. Сортируются по `online_since`
3. Очереди восстанавливаются в памяти

### Логирование
Все ключевые события логируются:
- Вход/выход водителя на линию
- Назначение заказа
- Принятие/отклонение
- Таймауты
- Переход в fallback

## 🐛 Отладка

### Проверка очередей
```python
from bot.services.queue_manager import queue_manager

# Информация о всех очередях
info = queue_manager.get_all_queues_info()
print(info)

# Позиция конкретного водителя
position = queue_manager.get_queue_position(driver_id)
```

### Проверка активных таймеров
```python
from bot.services.scheduler import scheduler

stats = scheduler.get_stats()
print(stats)
```

### Проверка статуса водителя
```sql
SELECT id, status, current_zone, online_since, pending_order_id
FROM drivers
WHERE user_id = ?;
```

## 📝 Checklist перед запуском

- [ ] Создана резервная копия БД
- [ ] Запущена миграция `add_queue_system.py`
- [ ] Проверено отсутствие ошибок миграции
- [ ] Все 11 водителей добавлены через `add_bulk_drivers.py`
- [ ] Бот перезапущен
- [ ] Проверено восстановление очередей в логах

## 🎉 Готово!

Система готова к работе. Водители могут выходить на линию, клиенты — создавать заказы. Всё распределение происходит автоматически.

---

**Версия:** 1.0.0  
**Дата:** 13.11.2024  
**Автор:** AI-архитектор Cursor

