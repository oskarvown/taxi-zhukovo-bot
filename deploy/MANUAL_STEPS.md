# 📋 Ручной деплой - Пошаговые команды

Если автоматический скрипт не работает, выполните эти команды **вручную через Web SSH** в панели Timeweb.

---

## 🔗 Шаг 1: Откройте Web SSH

1. Зайдите в панель Timeweb: https://timeweb.com/cloud
2. Найдите сервер с IP: **195.133.73.49**
3. Нажмите **"Подключиться"** или **"Web SSH"**
4. Откроется терминал в браузере

---

## 📦 Шаг 2: Установка пакетов

Скопируйте и выполните:

```bash
apt update && apt install -y python3 python3-venv python3-pip git nano
```

---

## 📥 Шаг 3: Клонирование проекта

```bash
cd /opt
git clone https://github.com/oskarvown/taxi-zhukovo-bot.git taxi-zhukovo
cd taxi-zhukovo
```

---

## 📝 Шаг 4: Создание .env файла

```bash
nano .env
```

**Вставьте это содержимое** (Ctrl+Shift+V для вставки в браузере):

```env
TELEGRAM_BOT_TOKEN=8447881195:AAFvBWR45SFXSy-lyeXfxnJWnVXrtAVVj1M
TELEGRAM_WEBHOOK_URL=
DATABASE_URL=sqlite:///./taxi_zhukovo.db
DEBUG=False
LOG_LEVEL=INFO
ADMIN_TELEGRAM_IDS=6840100810
BASE_PRICE=100.0
PRICE_PER_KM=25.0
MIN_PRICE=150.0
SERVICE_AREA_LAT=55.5833
SERVICE_AREA_LON=36.7500
SERVICE_RADIUS_KM=50.0
```

**Сохранение:**
- `Ctrl+O` (сохранить)
- `Enter` (подтвердить)
- `Ctrl+X` (выйти)

---

## 🚀 Шаг 5: Запуск деплоя

```bash
chmod +x deploy/deploy.sh
./deploy/deploy.sh
```

Подождите 2-3 минуты, пока скрипт выполнится.

---

## ▶️ Шаг 6: Запуск бота

```bash
systemctl start taxi-bot
systemctl enable taxi-bot
systemctl status taxi-bot
```

---

## ✅ Шаг 7: Проверка

```bash
journalctl -u taxi-bot -f
```

Должно появиться: `✅ Бот запущен и работает!`

**Проверка в Telegram:**
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Если бот отвечает - **всё работает!** 🎉

---

## 🆘 Если что-то пошло не так

### Просмотр логов:
```bash
journalctl -u taxi-bot -n 50
```

### Перезапуск:
```bash
systemctl restart taxi-bot
```

### Проверка .env:
```bash
cat /opt/taxi-zhukovo/.env
```

---

**Все команды готовы для копирования! Просто выполняйте их по порядку в Web SSH.**






