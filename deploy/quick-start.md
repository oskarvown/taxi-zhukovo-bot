# ⚡ Быстрый старт: Деплой бота за 15 минут

Это краткая инструкция для тех, кто хочет быстро разместить бота на сервере.

---

## 📋 Чек-лист перед началом

- [ ] Есть VPS сервер с Ubuntu 22.04
- [ ] Есть доступ к серверу по SSH
- [ ] Есть GitHub/GitLab репозиторий (или готовы создать)
- [ ] Есть Telegram Bot Token
- [ ] Есть ваш Telegram ID для админа

---

## 🚀 Шаг 1: Выбор и настройка сервера (5 минут)

1. **Выберите провайдера:**
   - 🇷🇺 **Timeweb** (рекомендую для России): https://timeweb.com/cloud
   - Или любой другой из [SERVER_SETUP.md](./SERVER_SETUP.md)

2. **Закажите VPS:**
   - 1 CPU, 1GB RAM, 20GB SSD
   - ОС: Ubuntu 22.04 LTS

3. **Подключитесь:**
   ```bash
   ssh root@ВАШ_IP_АДРЕС
   ```

---

## 📦 Шаг 2: Настройка Git репозитория (3 минуты)

### На вашем компьютере:

```bash
cd /Volumes/PortableSSD/TAXI_ЖУКОВО

# Если Git еще не инициализирован
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на GitHub и подключите:
git remote add origin https://github.com/oskarvown/taxi-zhukovo-bot.git
git branch -M main
git push -u origin main
```

**Готово!** Теперь код в GitHub.

---

## 🔧 Шаг 3: Деплой на сервер (5 минут)

### На сервере:

```bash
# 1. Установка необходимых пакетов
sudo apt update
sudo apt install -y python3 python3-venv python3-pip git

# 2. Создание пользователя
sudo adduser taxi
sudo usermod -aG sudo taxi

# 3. Клонирование репозитория
sudo mkdir -p /opt
cd /opt
sudo git clone https://github.com/oskarvown/taxi-zhukovo-bot.git taxi-zhukovo
cd taxi-zhukovo

# 4. Создание .env файла
sudo nano .env
```

**Содержимое .env:**
```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
ADMIN_TELEGRAM_IDS=ваш_telegram_id
DATABASE_URL=sqlite:///./taxi_zhukovo.db
LOG_LEVEL=INFO
```

**Сохраните файл:** `Ctrl+O`, `Enter`, `Ctrl+X`

### Запуск скрипта деплоя:

```bash
# 5. Запуск автоматического деплоя
sudo chmod +x deploy/deploy.sh
sudo ./deploy/deploy.sh
```

Скрипт автоматически:
- ✅ Создаст виртуальное окружение
- ✅ Установит зависимости
- ✅ Настроит systemd service
- ✅ Настроит права доступа

### Запуск бота:

```bash
# 6. Запуск бота
sudo systemctl start taxi-bot
sudo systemctl enable taxi-bot

# 7. Проверка статуса
sudo systemctl status taxi-bot

# 8. Просмотр логов
sudo journalctl -u taxi-bot -f
```

---

## ✅ Готово! Проверка работы

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Если бот отвечает - **всё работает!** 🎉

---

## 🔄 Обновление бота в будущем

### Процесс обновления:

**1. На вашем компьютере (после изменений через AI):**
```bash
cd /Volumes/PortableSSD/TAXI_ЖУКОВО
git add .
git commit -m "Описание изменений"
git push origin main
```

**2. На сервере:**
```bash
ssh user@your-server
cd /opt/taxi-zhukovo
sudo ./deploy/update.sh main
```

**Всё! Бот обновлен за 30 секунд!** ⚡

---

## 📞 Полезные команды для управления

```bash
# Статус бота
sudo systemctl status taxi-bot

# Остановка
sudo systemctl stop taxi-bot

# Запуск
sudo systemctl start taxi-bot

# Перезапуск
sudo systemctl restart taxi-bot

# Логи в реальном времени
sudo journalctl -u taxi-bot -f

# Последние 50 строк логов
sudo journalctl -u taxi-bot -n 50
```

---

## 🆘 Если что-то пошло не так

### Бот не запускается:

```bash
# Проверьте логи
sudo journalctl -u taxi-bot -n 50

# Проверьте .env файл
sudo cat /opt/taxi-zhukovo/.env

# Проверьте токен бота в Telegram (@BotFather)
```

### Ошибка "Conflict":

Бот уже запущен в другом месте. Решение:
```bash
# Найдите все процессы
ps aux | grep run.py

# Остановите все
sudo pkill -f run.py

# Подождите 1-2 минуты и перезапустите
sudo systemctl restart taxi-bot
```

### Обновление не работает:

```bash
# Проверьте права доступа
sudo chown -R taxi:taxi /opt/taxi-zhukovo

# Проверьте Git репозиторий
cd /opt/taxi-zhukovo
git status
git remote -v
```

---

## 📚 Полная документация

Для более детальной информации см.:
- [SERVER_SETUP.md](./SERVER_SETUP.md) - Выбор и настройка сервера
- [GIT_WORKFLOW.md](./GIT_WORKFLOW.md) - Работа с Git
- [DEPLOY.md](../DEPLOY.md) - Полное руководство по деплою

---

**Удачного деплоя! 🚀**

Если возникнут вопросы - проверьте логи и документацию выше.

