# 🔧 Исправление профиля водителя

## Проблема
Водитель не видит свой профиль в боте.

## Причины
1. Роль пользователя не установлена как `DRIVER`
2. Водитель не верифицирован (`is_verified = False`)
3. Нет связи между пользователем и профилем водителя

---

## ✅ Решение на сервере

### Вариант 1: Автоматическое исправление (рекомендуется)

**На сервере (через Web SSH):**

```bash
cd /opt/taxi-zhukovo
source venv/bin/activate

# Проверка профиля
python check_driver.py 7003530057

# Исправление профиля
python fix_driver_profile.py 7003530057

# Перезапуск бота
systemctl restart taxi-bot
```

### Вариант 2: Ручное исправление через Python

```bash
cd /opt/taxi-zhukovo
source venv/bin/activate
python3
```

В Python:

```python
from database.db import SessionLocal
from bot.models.driver import Driver
from bot.models.user import User, UserRole

db = SessionLocal()

# Находим пользователя
user = db.query(User).filter(User.telegram_id == 7003530057).first()

if user:
    # Устанавливаем роль водителя
    user.role = UserRole.DRIVER
    
    # Находим водителя
    driver = db.query(Driver).filter(Driver.user_id == user.id).first()
    
    if driver:
        # Верифицируем водителя
        driver.is_verified = True
        db.commit()
        print(f"✅ Профиль исправлен для {user.full_name}")
    else:
        print("❌ Профиль водителя не найден")
else:
    print("❌ Пользователь не найден")

db.close()
exit()
```

Затем перезапустите бота:

```bash
systemctl restart taxi-bot
```

---

## 🔍 Проверка

После исправления:

1. **Попросите водителя отправить `/start` в боте**
2. **Должно появиться меню водителя**

Если не помогло:

```bash
# Проверьте логи бота
journalctl -u taxi-bot -f

# Проверьте профиль еще раз
python check_driver.py 7003530057
```

---

## 📋 Информация о водителе

- **Имя**: Big Boss
- **Telegram ID**: 7003530057
- **Автомобиль**: LIFAN (Р633ВА102)

---

**После исправления водитель должен увидеть меню водителя!**

