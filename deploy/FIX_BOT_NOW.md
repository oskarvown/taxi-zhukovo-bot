# 🚨 БЫСТРОЕ ИСПРАВЛЕНИЕ - Бот не работает

## Если бот не запускается после обновления

### На сервере выполните:

```bash
# 1. Перейти в проект
cd /путь/к/проекту/TAXI_ЖУКОВО

# 2. Обновить код (важно!)
git pull origin main

# 3. Запустить диагностику
python3 check_bot_fix.py

# 4. Если есть ошибки - исправить
# Проверить, что DriverZone импортируется:
python3 -c "from bot.models import DriverZone; print('OK')"

# 5. Перезапустить бота
sudo systemctl restart taxi-bot

# 6. Проверить логи
sudo journalctl -u taxi-bot -f
```

---

## Основная проблема и решение

**Проблема:** `DriverZone` не импортируется

**Быстрое исправление:**

1. Отредактировать файл:
   ```bash
   nano bot/models/__init__.py
   ```

2. Найти строку:
   ```python
   from .driver import Driver, DriverStatus
   ```

3. Изменить на:
   ```python
   from .driver import Driver, DriverStatus, DriverZone
   ```

4. Найти `__all__` и добавить:
   ```python
   __all__ = [
       "User",
       "UserRole",
       "Driver",
       "DriverStatus",
       "DriverZone",  # <- добавить эту строку
       ...
   ]
   ```

5. Сохранить (Ctrl+O, Enter, Ctrl+X)

6. Перезапустить бота:
   ```bash
   sudo systemctl restart taxi-bot
   ```

---

## Если ничего не помогает - откат

```bash
# Вернуться к предыдущей версии
git reset --hard HEAD~2

# Перезапустить
sudo systemctl restart taxi-bot
```

---

**Подробная инструкция:** см. `ИСПРАВЛЕНИЕ_ПРОБЛЕМ_БОТА.md`

