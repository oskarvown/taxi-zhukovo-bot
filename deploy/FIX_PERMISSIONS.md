# 🔧 Исправление прав доступа

## Проблема
Скрипт не имеет прав на выполнение: `Permission denied`

## ✅ Решение

```bash
cd /opt/taxi-zhukovo

# Добавить права на выполнение
chmod +x deploy/deploy.sh

# Теперь запустить деплой
./deploy/deploy.sh
```

---

## 🚀 Все команды одной строкой

```bash
cd /opt/taxi-zhukovo && chmod +x deploy/deploy.sh && ./deploy/deploy.sh
```

---

**После этого скрипт должен запуститься!**














