# ✅ Проверка статуса бота после перезапуска

## Что видно из логов

✅ **Бот запущен!**
```
[  OK  ] Started taxi-bot.service - Taxi Zhukovo Telegram Bot.
```

---

## Проверка работы бота

### 1. Проверка статуса

```bash
systemctl status taxi-bot
```

**Должно показать:**
- `Active: active (running)`
- `Main PID: <число>`

### 2. Проверка логов бота

```bash
journalctl -u taxi-bot -n 50 --no-pager
```

**Должно показать:**
- `✅ Бот запущен и работает!`
- `Инициализация системы очередей...`
- `Order Dispatcher инициализирован`
- Без ошибок

### 3. Проверка в реальном времени

```bash
journalctl -u taxi-bot -f
```

Нажмите `Ctrl+C` для выхода.

---

## Если всё хорошо

- ✅ Бот запущен и работает
- ✅ Сервер работает нормально
- ✅ Все системные службы активны

---

## Если есть проблемы

### Бот не работает

```bash
# Проверка статуса
systemctl status taxi-bot

# Просмотр ошибок
journalctl -u taxi-bot -n 100 | grep -i error

# Перезапуск
systemctl restart taxi-bot
```

### Проверка базы данных

```bash
cd /opt/taxi-zhukovo
ls -lh taxi_zhukovo.db
```

---

**Если бот запущен (как видно из логов) - всё отлично! 🎉**




