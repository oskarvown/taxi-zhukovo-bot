# 🚀 Деплой на Timeweb - Пошаговая инструкция

Это подробная инструкция для деплоя бота на сервер Timeweb.

---

## 📋 Что вам понадобится

- ✅ Сервер Timeweb (уже оплачен)
- ✅ IP-адрес сервера
- ✅ Пароль root (из панели Timeweb)
- ✅ Telegram Bot Token (у вас уже есть в setup_env.py)
- ✅ Ваш Telegram ID (6840100810)

---

## 🔑 Шаг 1: Получение доступа к серверу

1. **Зайдите в панель Timeweb:**
   - https://timeweb.com/cloud
   - Войдите в аккаунт

2. **Найдите ваш сервер:**
   - Перейдите в "Облачные серверы"
   - Найдите ваш VPS

3. **Получите данные для подключения:**
   - **IP-адрес**: будет показан в списке серверов
   - **Пароль root**: в разделе "Пароли" или при создании сервера
   - Если пароля нет - сбросьте его в панели

---

## 💻 Шаг 2: Подключение к серверу

### Windows:
Используйте **PuTTY** или **Windows Terminal**:

```bash
ssh root@ВАШ_IP_АДРЕС
# Введите пароль при запросе
```

### Mac/Linux:
```bash
ssh root@ВАШ_IP_АДРЕС
# Введите пароль при запросе
```

**Пример:**
```bash
ssh root@123.45.67.89
```

---

## 🔧 Шаг 3: Первоначальная настройка сервера

После подключения выполните:

```bash
# 1. Обновление системы
apt update && apt upgrade -y

# 2. Установка необходимых пакетов
apt install -y python3 python3-venv python3-pip git nano

# 3. Проверка версии Python (должна быть 3.10+)
python3 --version
```

---

## 📦 Шаг 4: Клонирование проекта

```bash
# 1. Создание директории
mkdir -p /opt
cd /opt

# 2. Клонирование репозитория
git clone https://github.com/oskarvown/taxi-zhukovo-bot.git taxi-zhukovo

# 3. Переход в директорию проекта
cd taxi-zhukovo
```

---

## ⚙️ Шаг 5: Создание .env файла

```bash
# Создание .env файла
nano .env
```

**Вставьте следующее содержимое** (замените на ваши данные):

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=8447881195:AAFvBWR45SFXSy-lyeXfxnJWnVXrtAVVj1M
TELEGRAM_WEBHOOK_URL=

# Database Configuration
DATABASE_URL=sqlite:///./taxi_zhukovo.db

# Application Settings
DEBUG=False
LOG_LEVEL=INFO

# Admin Configuration
ADMIN_TELEGRAM_IDS=6840100810

# Pricing Configuration
BASE_PRICE=100.0
PRICE_PER_KM=25.0
MIN_PRICE=150.0

# Service Area Configuration (Жуково)
SERVICE_AREA_LAT=55.5833
SERVICE_AREA_LON=36.7500
SERVICE_RADIUS_KM=50.0
```

**Сохранение в nano:**
- Нажмите `Ctrl+O` (сохранить)
- Нажмите `Enter` (подтвердить имя файла)
- Нажмите `Ctrl+X` (выйти)

---

## 🚀 Шаг 6: Автоматический деплой

```bash
# 1. Переход в директорию проекта (если еще не там)
cd /opt/taxi-zhukovo

# 2. Запуск скрипта деплоя
chmod +x deploy/deploy.sh
./deploy/deploy.sh
```

Скрипт автоматически:
- ✅ Создаст пользователя `taxi`
- ✅ Создаст виртуальное окружение
- ✅ Установит все зависимости
- ✅ Настроит systemd service
- ✅ Настроит права доступа

**Время выполнения:** ~2-3 минуты

---

## ▶️ Шаг 7: Запуск бота

```bash
# 1. Запуск бота
systemctl start taxi-bot

# 2. Включение автозапуска при загрузке сервера
systemctl enable taxi-bot

# 3. Проверка статуса
systemctl status taxi-bot
```

**Если всё хорошо, вы увидите:**
```
● taxi-bot.service - Taxi Zhukovo Telegram Bot
     Loaded: loaded
     Active: active (running)
```

---

## 📊 Шаг 8: Проверка работы

### Просмотр логов в реальном времени:

```bash
journalctl -u taxi-bot -f
```

**Вы должны увидеть:**
```
✅ Бот запущен и работает!
```

### Проверка в Telegram:

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Если бот отвечает - **всё работает!** 🎉

---

## 🔄 Обновление бота в будущем

После изменений через AI и push в GitHub:

```bash
# На сервере
cd /opt/taxi-zhukovo
./deploy/update.sh main
```

Или вручную:

```bash
# 1. Остановка
systemctl stop taxi-bot

# 2. Обновление из Git
cd /opt/taxi-zhukovo
git pull origin main

# 3. Обновление зависимостей
source venv/bin/activate
pip install -r requirements.txt

# 4. Запуск
systemctl start taxi-bot
```

---

## 🆘 Решение проблем

### Бот не запускается

```bash
# Проверьте логи
journalctl -u taxi-bot -n 50

# Проверьте .env файл
cat /opt/taxi-zhukovo/.env

# Проверьте токен бота
```

### Ошибка "Permission denied"

```bash
# Установка правильных прав
chown -R taxi:taxi /opt/taxi-zhukovo
chmod -R u+rw /opt/taxi-zhukovo
```

### Ошибка "Conflict" (бот уже запущен)

```bash
# Найдите все процессы
ps aux | grep run.py

# Остановите все
pkill -f run.py

# Подождите 1-2 минуты и перезапустите
systemctl restart taxi-bot
```

### Не могу подключиться по SSH

1. Проверьте IP-адрес в панели Timeweb
2. Убедитесь, что используете правильный пароль
3. Попробуйте сбросить пароль в панели Timeweb

---

## 📞 Полезные команды

```bash
# Статус бота
systemctl status taxi-bot

# Остановка
systemctl stop taxi-bot

# Запуск
systemctl start taxi-bot

# Перезапуск
systemctl restart taxi-bot

# Логи (последние 100 строк)
journalctl -u taxi-bot -n 100

# Логи в реальном времени
journalctl -u taxi-bot -f

# Проверка использования ресурсов
htop
# (установить: apt install htop)

# Проверка дискового пространства
df -h
```

---

## ✅ Чек-лист готовности

После деплоя проверьте:

- [ ] Бот запущен: `systemctl status taxi-bot` показывает `active (running)`
- [ ] Логи показывают: `✅ Бот запущен и работает!`
- [ ] Бот отвечает в Telegram на команду `/start`
- [ ] Автозапуск включен: `systemctl is-enabled taxi-bot` показывает `enabled`

---

**Готово! Ваш бот работает на сервере Timeweb! 🎉**

Если возникнут проблемы - проверьте логи и обратитесь к разделу "Решение проблем" выше.






