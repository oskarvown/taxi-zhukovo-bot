# 🔄 Workflow работы с Git для обновлений бота

Этот документ описывает простой процесс обновления бота через Git, чтобы вы могли легко вносить изменения через AI (Cursor) и применять их на сервере.

---

## 🎯 Стратегия: простота и надежность

**Основная идея:**
1. Вы работаете локально (или через Cursor AI)
2. Коммитите изменения в Git
3. Пушите в удаленный репозиторий (GitHub/GitLab)
4. На сервере делаете `git pull` и перезапускаете бота

**Это самый простой и надежный способ!**

---

## 📦 Шаг 1: Настройка Git репозитория

### Вариант A: GitHub (рекомендуется)

1. **Создайте репозиторий на GitHub:**
   - Зайдите на https://github.com
   - Нажмите "New repository"
   - Название: `taxi-zhukovo-bot`
   - Выберите **Private** (приватный)
   - НЕ добавляйте README, .gitignore и лицензию (они уже есть)

2. **Подключите локальный проект к GitHub:**

```bash
# В корне вашего проекта
cd /Volumes/PortableSSD/TAXI_ЖУКОВО

# Инициализация Git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Taxi Zhukovo Bot"

# Добавление удаленного репозитория
git remote add origin https://github.com/oskarvown/taxi-zhukovo-bot.git

# Отправка в GitHub
git branch -M main
git push -u origin main
```

### Вариант B: GitLab

Аналогично GitHub, только на https://gitlab.com

### Вариант C: Локальный Git на сервере (без GitHub)

Если не хотите использовать GitHub/GitLab, можно использовать Git только на сервере:

```bash
# На сервере
cd /opt/taxi-zhukovo
git init
git add .
git commit -m "Initial commit"
```

Но в этом случае обновления придется делать вручную через SCP.

---

## 🔧 Шаг 2: Настройка .gitignore

Убедитесь, что `.gitignore` содержит:

```gitignore
# Секретные данные - НЕ коммитим!
.env
*.db
*.sqlite

# Виртуальное окружение
venv/
env/

# Логи
*.log
logs/

# Кэш Python
__pycache__/
*.pyc

# Резервные копии
backups/
```

**⚠️ ВАЖНО:** Никогда не коммитьте:
- `.env` файл (токен бота!)
- Базы данных `*.db`
- Резервные копии

---

## 🚀 Шаг 3: Первоначальный деплой с Git

### На сервере:

```bash
# Клонирование репозитория
cd /opt
sudo git clone https://github.com/oskarvown/taxi-zhukovo-bot.git taxi-zhukovo
cd taxi-zhukovo

# Установка зависимостей
sudo python3 -m venv venv
sudo venv/bin/pip install -r requirements.txt

# Создание .env файла (НЕ из Git!)
sudo nano .env
# Добавьте ваш TELEGRAM_BOT_TOKEN и другие настройки

# Запуск скрипта деплоя
sudo chmod +x deploy/deploy.sh
sudo ./deploy/deploy.sh
```

---

## 🔄 Шаг 4: Процесс обновления бота

### Вариант A: Автоматический скрипт (рекомендуется) ⭐

**На сервере:**

```bash
# Обновление бота одной командой
cd /opt/taxi-zhukovo
sudo ./deploy/update.sh main
```

Скрипт автоматически:
1. ✅ Создаст резервную копию БД
2. ✅ Остановит бота
3. ✅ Обновит код из Git
4. ✅ Обновит зависимости
5. ✅ Запустит бота

### Вариант B: Ручное обновление

```bash
# 1. Остановка бота
sudo systemctl stop taxi-bot

# 2. Резервная копия БД
sudo cp /opt/taxi-zhukovo/taxi_zhukovo.db /opt/taxi-zhukovo/backups/taxi_zhukovo_$(date +%Y%m%d_%H%M%S).db

# 3. Обновление из Git
cd /opt/taxi-zhukovo
sudo -u taxi git fetch origin
sudo -u taxi git pull origin main

# 4. Обновление зависимостей
sudo -u taxi venv/bin/pip install -r requirements.txt

# 5. Запуск бота
sudo systemctl start taxi-bot

# 6. Проверка
sudo systemctl status taxi-bot
```

---

## 💻 Workflow: Работа с AI (Cursor)

### Типичный процесс обновления:

1. **Вы просите AI внести изменения:**
   ```
   "Измени функцию X в файле Y"
   ```

2. **AI вносит изменения в локальные файлы**

3. **Проверка изменений:**
   ```bash
   git status
   git diff
   ```

4. **Коммит изменений:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   ```

5. **Отправка в GitHub:**
   ```bash
   git push origin main
   ```

6. **На сервере - обновление:**
   ```bash
   cd /opt/taxi-zhukovo
   sudo ./deploy/update.sh main
   ```

7. **Проверка работы:**
   ```bash
   sudo journalctl -u taxi-bot -f
   ```

---

## 🔐 Безопасность: SSH ключи для Git

Для удобства можно настроить SSH ключи для GitHub:

### 1. Генерация SSH ключа (на вашем компьютере)

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
# Нажмите Enter для сохранения в ~/.ssh/id_ed25519
```

### 2. Добавление ключа в GitHub

```bash
# Скопируйте публичный ключ
cat ~/.ssh/id_ed25519.pub

# На GitHub:
# Settings → SSH and GPG keys → New SSH key
# Вставьте скопированный ключ
```

### 3. Использование SSH вместо HTTPS

```bash
# Изменение URL репозитория
git remote set-url origin git@github.com:oskarvown/taxi-zhukovo-bot.git
```

---

## 📋 Чек-лист перед коммитом

Перед каждым коммитом убедитесь:

- [ ] `.env` файл НЕ добавлен в коммит
- [ ] База данных `*.db` НЕ добавлена в коммит
- [ ] Изменения протестированы локально (если возможно)
- [ ] Описание коммита понятное

Проверка:
```bash
git status
# Убедитесь, что .env и *.db файлы не в списке
```

---

## 🛠️ Полезные Git команды

```bash
# Просмотр изменений
git status
git diff

# Отмена изменений в файле (до git add)
git checkout -- filename.py

# Отмена git add (но сохранение изменений)
git reset HEAD filename.py

# Просмотр истории
git log --oneline

# Создание новой ветки (для экспериментов)
git checkout -b feature/new-feature

# Возврат к определенному коммиту (если что-то сломалось)
git checkout <commit-hash>
```

---

## 🔄 Быстрый workflow (копипаста)

### Локально (после изменений через AI):

```bash
cd /Volumes/PortableSSD/TAXI_ЖУКОВО
git add .
git commit -m "Описание изменений"
git push origin main
```

### На сервере:

```bash
ssh user@your-server
cd /opt/taxi-zhukovo
sudo ./deploy/update.sh main
sudo journalctl -u taxi-bot -f
```

**Всё! Бот обновлен за 2 минуты! 🚀**

---

## 🆘 Решение проблем

### Ошибка: "Permission denied" при git pull

```bash
# Установка правильных прав
sudo chown -R taxi:taxi /opt/taxi-zhukovo
sudo chmod -R u+rw /opt/taxi-zhukovo
```

### Ошибка: "Merge conflict"

```bash
# Просмотр конфликтов
git status

# Если нужно отменить локальные изменения и взять с сервера:
git reset --hard origin/main

# Или разрешить конфликты вручную в файлах
```

### Откат к предыдущей версии

```bash
# Просмотр истории
git log --oneline

# Откат к определенному коммиту
git checkout <commit-hash>
git push origin main --force  # ⚠️ Используйте осторожно!
```

---

## 📚 Дополнительные ресурсы

- [Git документация](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)

---

**Теперь вы можете легко обновлять бота через Git! 🎉**

