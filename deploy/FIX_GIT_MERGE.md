# 🔧 Исправление конфликта Git

## Проблема
На сервере есть локальные изменения, которые конфликтуют с обновлениями из GitHub.

## ✅ Решение

### Вариант 1: Отменить локальные изменения (рекомендуется)

```bash
cd /opt/taxi-zhukovo

# Отменить локальные изменения и взять версию из GitHub
git reset --hard origin/main

# Теперь можно запустить деплой
./deploy/deploy.sh
```

### Вариант 2: Сохранить изменения во временное хранилище

```bash
cd /opt/taxi-zhukovo

# Сохранить локальные изменения
git stash

# Обновить из GitHub
git pull origin main

# Применить сохраненные изменения (если нужно)
git stash pop
```

### Вариант 3: Принудительное обновление

```bash
cd /opt/taxi-zhukovo

# Отменить все локальные изменения
git checkout -- deploy/deploy.sh

# Обновить из GitHub
git pull origin main
```

---

## 🚀 Рекомендуемый порядок действий

```bash
cd /opt/taxi-zhukovo

# 1. Отменить локальные изменения
git reset --hard origin/main

# 2. Убедиться, что всё обновлено
git pull origin main

# 3. Запустить деплой
./deploy/deploy.sh

# 4. Запустить бота
systemctl start taxi-bot
systemctl enable taxi-bot
systemctl status taxi-bot
```

---

**После этого всё должно работать!**






