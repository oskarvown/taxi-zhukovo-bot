# 🚀 Быстрое обновление на сервере

## Команды (выполнить по порядку):

```bash
# 1. Подключиться к серверу
ssh ваш_пользователь@ваш_сервер

# 2. Перейти в проект
cd /путь/к/проекту/TAXI_ЖУКОВО

# 3. Создать бэкап БД
mkdir -p backups
cp taxi_zhukovo.db backups/taxi_zhukovo_backup_$(date +%Y%m%d_%H%M%S).db

# 4. Остановить бота
sudo systemctl stop taxi-bot
# или если используете screen/tmux: найдите и остановите процесс

# 5. Обновить код
git pull origin main

# 6. Перезапустить бота
sudo systemctl start taxi-bot

# 7. Проверить логи
sudo journalctl -u taxi-bot -f
```

## Проверка новых команд:

В Telegram боте (от имени администратора):
- `/reset_drivers` - сброс состояния всех водителей
- `/check_dema` - проверка водителей в зоне DEMA

---

**Подробная инструкция:** см. `deploy/UPDATE_QUEUE_AUDIT.md`

